node_modules
.next
.git
.github
coverage

# `tests/` and `scripts/` ARE part of the build context: the `tester` stage runs
# the suite and typechecks the whole project. Neither reaches the final image,
# which only copies .next/standalone and public/.

*.md
!README.md
.env
.env.local
.env*.local
.deploy.env
firebase-debug.log
.firebase
Dockerfile
.dockerignore
